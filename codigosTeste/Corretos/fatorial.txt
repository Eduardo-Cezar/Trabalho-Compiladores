	//função fatorial
      func int fat(int n){
		if (n == 1) then {
                  return 1;
		}else{
			return n * call fat (n-1);
		}
	}
      //função principal
	func int main(){
		vars{
			int n;
		}

		in(n);
		out(call fat(n));

		return 0;
	}